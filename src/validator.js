const validator = {
  isValid(creditCard) {
    // algoritmo que valida creditcard
    return false // aplicar algoritmo
  },
  maskify(creditCard) {
   // solo muestra los ultimos 4 digitos
   return creditCard // aplicar algoritmo
  }
};

export default validator;
